// TODO: type uuid
// TODO: why no matter if default or not ? i think it does because t can't find it in container any more
export type Task = { id: string; title: string; time: Date } // TODO: how to disallow additional props?
